import { DataLoadDirective } from './data-load.directive';

describe('DataLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new DataLoadDirective();
    expect(directive).toBeTruthy();
  });
});
