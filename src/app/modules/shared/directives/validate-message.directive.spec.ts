import { ValidateMessageDirective } from './validate-message.directive';

describe('ValidateMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateMessageDirective();
    expect(directive).toBeTruthy();
  });
});
